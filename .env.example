# Server Configuration
PORT=3005
NODE_ENV=development

# MongoDB
# For local development:
MONGODB_URI=mongodb://localhost:27017/managarr
# For Docker Compose (uses internal network):
# MONGODB_URI=mongodb://mongo:27017/managarr

# Client URL (for CORS - only needed in development)
CLIENT_URL=http://localhost:5179

# Vite Dev Server (client - only used in development)
VITE_PORT=5179
VITE_API_URL=http://localhost:3005

# ===========================================
# Docker Notes:
# When using docker-compose, environment variables
# are set in docker-compose.yml, not here.
# This file is for local development only.
# ===========================================
