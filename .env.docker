# Managarr Docker Environment Variables
# Set these in Portainer's environment variables section

NODE_ENV=production
PORT=3005

# MongoDB connection (individual variables)
MONGODB_HOST=192.168.1.1
MONGODB_PORT=27017
MONGODB_USER=admin
MONGODB_PASSWORD=password
MONGODB_DATABASE=managarr

# Or use a full connection string instead (overrides individual vars)
# MONGODB_URI=mongodb://user:password@host:27017/managarr?authSource=admin
