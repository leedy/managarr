services:
  managarr:
    build: .
    container_name: managarr
    restart: unless-stopped
    ports:
      - "3005:3005"
    environment:
      - NODE_ENV=production
      - PORT=3005
      - MONGODB_URI=${MONGODB_URI:-mongodb://host.docker.internal:27017/managarr}
